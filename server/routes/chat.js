// fetchChannelsByCityIdByUserId

import { Router } from "express";
// import ChatController from "../controllers/chat-controller.js";

const chat = Router();
// chat.get('/channel', ChatController.getChannels);

export default chat;